syntax on

if !isdirectory("~/.vimbackupdir")
  call mkdir("~/.vimbackupdir", "p")
endif

set backupdir=~/.vimbackupdir,~/tmp,~/,.
set directory=~/.vimbackupdir,~/tmp,~/,.

set autoindent
set expandtab
set shiftwidth=2
set softtabstop=2
set tabstop=2
